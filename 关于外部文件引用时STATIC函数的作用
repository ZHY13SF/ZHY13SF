#define _CRT_SECURE_NO_WARNINGS

static int Add(int x, int y)
{
	return x + y;
}



 The follow is another  file
 
#include<stdio.h>
extern int Add(int, int);
int main() 
{
	int a = 50;
	int b = 30;
	int sum = Add(a, b);
	printf("%d", sum);
	return 0;


} 
此时此文件无法从外部引用Add函数 系统报错
