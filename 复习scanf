#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main() {
	int age = 0;
	printf("请输入您要查询的年龄");
	
	scanf("%d", &age);
	if (age < 18)
		printf("未成年\n");
	else if (age >= 18 && age <= 26)
		printf("正是该奋斗的年龄");
	else if (age >= 26 && age <= 34)
		printf("当打之年");  
	


	return 0;	
}
